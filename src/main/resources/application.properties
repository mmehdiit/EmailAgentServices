# Server
server.port=${SERVER_PORT:8080}

# Database - PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://192.168.55.115:5432/emailagent}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# JWT
app.jwt.secret=${JWT_SECRET:8e215bd826df4fb9a2fb4b2c2aad7e779b17bbb783d294634448ac3090afde8d}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Microsoft OAuth (Outlook)
microsoft.client-id=${MICROSOFT_CLIENT_ID:ad5526fd-12bc-46f5-9fd7-d8334a77d81c}
microsoft.client-secret=${MICROSOFT_CLIENT_SECRET}
microsoft.redirect-uri=${MICROSOFT_REDIRECT_URI:http://localhost:4200/dashboard}

# AI Classification (Ollama - local)
ai.gateway.url=${AI_GATEWAY_URL:http://localhost:11434/v1/chat/completions}
ai.gateway.api-key=${AI_GATEWAY_API_KEY:ollama}
ai.gateway.model=${AI_GATEWAY_MODEL:qwen2.5:3b}

# App URL (for reply tracking links)
app.url=${APP_URL:http://localhost:4200}

# Scheduled tasks
app.processing.enabled=${PROCESSING_ENABLED:false}
app.processing.cron=${PROCESSING_CRON:0 */5 * * * *}
app.processing.thread-pool.core-size=${PROCESSING_THREAD_POOL_CORE:4}
app.processing.thread-pool.max-size=${PROCESSING_THREAD_POOL_MAX:8}
app.processing.thread-pool.queue-capacity=${PROCESSING_THREAD_POOL_QUEUE:50}
app.reply-check.enabled=${REPLY_CHECK_ENABLED:false}
app.reply-check.cron=${REPLY_CHECK_CRON:0 */10 * * * *}

# OCR Gateway
app.ocr.url=${OCR_URL:http://localhost:8014}
app.ocr.auth.url=${OCR_AUTH_URL:http://localhost:8001}
app.ocr.auth.username=${OCR_AUTH_USERNAME:anthony.eltabbal@claims-express.net}
app.ocr.auth.password=${OCR_AUTH_PASSWORD:2z94wLlH5rR2KxMw}

# Call Center API
app.callcenter.url=${CALLCENTER_URL:http://localhost:8004}

# Data Management API
app.datamanagement.url=${DATAMANAGEMENT_URL:http://localhost:8005}

# Logging
logging.level.com.emailagent=${LOG_LEVEL_APP:DEBUG}
logging.level.org.springframework.security=${LOG_LEVEL_SECURITY:WARN}
