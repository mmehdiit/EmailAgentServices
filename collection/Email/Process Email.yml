info:
  name: Process Email
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/v1/api/emails/process
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
